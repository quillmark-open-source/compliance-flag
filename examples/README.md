# Examples

This directory contains sample Compliance Flag inputs and generated report
artifacts.

The initial HTML fixture used by tests is in `tests/fixtures/example-blog-post.html`.

## Reports

`reports/example-blog-post-report.*` is a sanitized sample report generated from
the fictitious HTML fixture. The sample uses repo-relative source paths so it can
be committed without exposing a maintainer's local filesystem layout.
